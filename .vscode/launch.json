{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run localhost",
      "type": "chrome",
      "request": "launch",
      "url": "http://127.0.0.1:5500",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Serve localhost"
    }
  ]
}
